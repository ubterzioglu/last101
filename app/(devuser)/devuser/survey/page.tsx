import { SurveyClient } from './SurveyClient';

export const metadata = {
  title: 'Tanışma Kaynaşma Toplantısı - almanya101',
  robots: { index: false, follow: false },
};

export default function SurveyPage() {
  return <SurveyClient />;
}
