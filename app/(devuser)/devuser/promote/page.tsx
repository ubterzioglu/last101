import { PromoteClient } from './PromoteClient';

export const metadata = {
  title: 'Promote Your Product - almanya101',
  robots: { index: false, follow: false },
};

export default function PromotePage() {
  return <PromoteClient />;
}
