# Debug Mode Rules

## Known Placeholders

- `lib/supabase/client.ts` and `lib/supabase/server.ts` return `null` - NOT implemented. Any Supabase-related errors are expected.

## Environment

- Check `.env.local` for environment variables (not committed to git)
- Dev server runs on `http://localhost:3000` via `npm run dev`
