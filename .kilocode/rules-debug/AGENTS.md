# Debug Mode Rules

## Next.js Development

- Dev server runs on `http://localhost:3000` via `npm run dev`
- Turbopack is enabled by default in development
- Check `.env.local` for environment variables (not committed to git)

## Known Placeholders

- `lib/supabase/client.ts` and `lib/supabase/server.ts` are NOT implemented - any Supabase errors are expected
