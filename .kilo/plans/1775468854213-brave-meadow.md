# Implementation Plan

## 1. "Nasıl çalışır" Cards in Tools
The user requested that "Nasıl çalışır" cards be separate in:
- Para Transferi Seçim Aracı
- Almanya Tatil Planlayıcı
- Türkiye Tatil Planlayıcı

**Observation:** In the current local codebase, these are *already* separate cards (separated in a recent commit). However, `ParaTransferi` currently uses `mb-2` for spacing while other tools (like `BankaClient`) use a `space-y-2` parent wrapper. 
**Action:** 
- I will refactor `components/para-transferi/transfer-selector.tsx` to use the exact `space-y-2` structure as `BankaClient.tsx` and remove all `mb-2` classes for perfect consistency.
- Ensure `GermanHolidayCalculator.tsx` and `HolidayCalculator.tsx` perfectly match the `space-y-2` separate card structure.

## 2. Hizmet Rehberi Refactoring
**File:** `app/(site)/hizmet-rehberi/page.tsx`

The user requested several changes to the Hizmet Rehberi page based on the live site:
1. **Merge Sections:** "hizmet ve gastronomi rehberi sectionları kalkacak tek section olacak filtreler solda merge olacak."
   *Observation:* This is already merged in the local codebase (the `PROVIDER_CATEGORIES` contains both). I will ensure the UI reflects a unified list.
2. **Hero Section Redesign:**
   - Change title to: "Türk Hizmet Rehberi"
   - Change subtitle to: "Almanya'da Türkçe hizmet veren doktorlardan restoranlara, tüm uzmanları tek yerde bulun."
   - Remove the "fazladan sayfa içinde header" (remove the extra badge/pre-title if it's redundant, and reduce top/bottom padding to make it as compact as possible).
   - Change Hero Padding: Reduce from `py-14 md:py-20` to `py-6 md:py-8` to eliminate empty space.
3. **Hero Stats (Kayıtlar):**
   - Shrink the stat cards (make them smaller).
   - Show 5 categories instead of 3. (e.g., Toplam, Doktor, Avukat, Restoran, Kuaför).
4. **White Theme Conversion:**
   - "tüm sayfa arkaplanı beyaz olacak ona göre de diğer kartlar ve filtreler ayarlanacak."
   - Convert the entire page (`hizmet-rehberi/page.tsx` and `hizmet-rehberi/oneri/HizmetOnerClient.tsx`) from dark mode (`bg-[#050505]`, `text-white`, `border-white/10`) to a clean light mode (`bg-gray-50`/`bg-white`, `text-gray-900`, `border-gray-200`).
   - Update sidebar filter backgrounds, search inputs, dropdowns, and provider cards to match the white theme.
   - Remove any dark mode gradients and replace them with light/soft borders.
